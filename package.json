{
  "name": "collectwise-atlas-lookup",
  "version": "1.0.0",
  "private": true,
  "description": "CSV ingestion + account lookup API for the Atlas Recovery AI agent",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "ingest": "node src/ingest.js",
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "smoke": "node scripts/smoke.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "csv-parse": "^5.6.0",
    "express": "^4.21.2"
  }
}
